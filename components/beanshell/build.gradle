version = '1.0'

dependencies {
	compile "bsh:bsh:+"
}

task copyToLib(type: Copy) {
	into "$buildDir/libs"
	from configurations.runtime
	exclude '**/ect-core*'
	exclude '**/equip*'
}

build.dependsOn(copyToLib)