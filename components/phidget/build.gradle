apply plugin: 'java'

version = '1.0'
archivesBaseName = 'phidget-components'

sourceSets
		{
			main {
				java {
					srcDir 'src'
				}
				resources {
					srcDir 'resources'
				}
			}
		}

dependencies
		{
			compile project(":core")
			compile files("lib/phidget21.jar")
		}