apply plugin: 'java'
apply plugin: 'application'

version = '1.0'
archivesBaseName = 'ect-core'
mainClassName = "equip.ect.apps.ExporterGUI"

sourceSets {
	main {
		java {
			srcDir 'src'
		}
	}
}

dependencies {
	compile project(":equip")
}